# Add context based custom code and imports here
# It's a bit inconvenient that this file always has to exist.
# Would be nice to just import everything inside an if statement
# but nushell does not allow that at the time of writing:
# https://github.com/nushell/nushell/issues/7203

